@if (!Request.IsAuthenticated)
{
    //@: Welcome <b>@User.Identity.Name</b>!
    @:<font color="white">  Welcome <b>James Li</b>!</font>
    @:<font color="white"> | </font>
    @Html.ActionLink("Logout", "LogOff", "Account", new { @style = "color:white;text-decoration: underline;" })
}
